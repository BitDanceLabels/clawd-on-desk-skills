# Clawd does not auto-load .env files.
# This sample documents the environment variables used by hook scripts and
# remote SSH workflows so they can be exported by your shell/service wrapper.

# Set on remote hosts to skip local PID/terminal detection in hooks.
# Leave unset for normal local desktop use.
CLAWD_REMOTE=1
